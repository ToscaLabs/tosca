[package]
name = "light-with-state"
version = "0.2.0"
edition = "2024"
rust-version = "1.96"
authors = ["Michele Valsesia <michele.valsesia@toscalabs.org>"]
description = "A simple stateful light for an ESP32-C3 board."
license = "Apache-2.0 OR MIT"
publish = false

[dependencies]
# Tosca crates
tosca = { path = "../../../tosca", version = "0.2.0", default-features = false }
tosca-esp32c3 = { path = "../../", version = "0.2.0" }

# Embassy framework
embassy-executor = { version = "0.10.0", features = ["log"] }
embassy-sync = "0.8.0"
embassy-time = { version = "0.5.1", features = ["log"] }

# ESP dependencies
esp-alloc = "0.11.0"
esp-bootloader-esp-idf = { version = "0.6.0", features = ["esp32c3", "log-04"] }
esp-hal = { version = "~1.2.2", features = ["esp32c3", "log-04", "unstable"] }
esp-rtos = { version = "0.4.0", features = ["embassy", "esp-alloc", "esp-radio", "esp32c3", "log-04"] }
esp-println = { version = "0.18.0", features = ["esp32c3", "log-04"] }
esp-radio = { version = "1.0.0-beta.1", features = ["esp-alloc", "esp32c3", "log-04", "wifi"] }

# Logging
log = "0.4.33"

# TOML configuration
toml-cfg = { version = "0.2.0", default-features = false }

[build-dependencies]
toml-cfg = { version = "0.2.0", default-features = false }

[profile.dev]
# Rust debug is too slow.
# For debug builds always builds with some optimization
opt-level = "s"

[profile.release]
codegen-units    = 1     # LLVM can perform better optimizations using a single thread
debug            = 2
debug-assertions = false
incremental      = false
lto              = 'fat'
opt-level        = 's'
overflow-checks  = false
